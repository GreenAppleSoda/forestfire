# ForestFire Express API

공개 웹 백엔드. Flask(ml-service)를 프록시하고 지도 JSON을 서빙합니다.

```powershell
copy .env.example .env
npm install
npm run dev
```

기본 포트: 4000  
Flask URL: `ML_SERVICE_URL` (기본 `http://127.0.0.1:5000`)
